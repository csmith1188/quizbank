<%- include('../partials/teacher-header', { title: 'New Quiz - ' + course.name, user, course, breadcrumb: '<a href="/courses/' + course.id + '">' + course.name + '</a> &rarr; <a href="/courses/' + course.id + '/quizzes">Quizzes</a> &rarr; New' }) %>
<h1>New Quiz</h1>
<form method="post" action="/courses/<%= course.id %>/quizzes">
    <label>Name <input type="text" name="name" required /></label>
    <div class="actions">
        <button type="submit" class="btn">Create</button>
        <a href="/courses/<%= course.id %>/quizzes" class="btn btn-secondary">Cancel</a>
    </div>
</form>
<%- include('../partials/footer') %>
